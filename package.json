{
  "name": "colppy-challenge",
  "private": true,
  "version": "1.0.0",
  "description": "Colppy Sales Dashboard — monorepo root",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "setup": "npm install && npm run install:all && npm run setup:env",
    "setup:env": "node scripts/setup-env.mjs",
    "dev": "concurrently -k -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "db:migrate": "npm run db:migrate --prefix backend",
    "test": "npm run test --prefix backend && npm run test --prefix frontend",
    "test:backend": "npm run test --prefix backend",
    "test:frontend": "npm run test --prefix frontend",
    "lint": "npm run lint --prefix backend && npm run lint --prefix frontend",
    "validate": "npm run validate --prefix backend && npm run lint --prefix frontend && npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
